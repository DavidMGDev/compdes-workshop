# Dependencias base del taller (Hora 1 y labs manuales).
# PyRIT y Garak van en SUS PROPIOS entornos virtuales (ver docs/SETUP.md):
# tienen conflictos de dependencias con estas y entre sí.
#
# Versiones fijadas al piso que se probó; «>=» permite parches menores.
openai>=1.30
# >=1.8: incluye el transporte streamable-http que usa Onyx (modo --http).
mcp>=1.8
pydantic>=2.7
# psycopg2-binary trae el driver de Postgres ya compilado (sin necesitar
# compilador ni libpq en la máquina del asistente — clave en Windows).
psycopg2-binary>=2.9
pdfplumber>=0.11
reportlab>=4.0
sentence-transformers>=3.0
numpy>=1.26
requests>=2.31

# Wrapper HTTP del agente (necesario para PyRIT/Garak/Promptfoo en Horas 2-3).
fastapi>=0.110
uvicorn>=0.29
